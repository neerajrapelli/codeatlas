{
  "name": "@codeatlas/ai",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "python -m compileall -q src && node -e \"require('node:fs').mkdirSync('dist',{recursive:true}); require('node:fs').writeFileSync('dist/buildstamp','')\"",
    "dev": "python -m uvicorn codeatlas_ai.main:app --reload --app-dir src --host 0.0.0.0 --port 8001",
    "lint": "python -m compileall -q src",
    "lint:ruff": "python -m ruff check src && python -m ruff format --check src",
    "typecheck": "python -m compileall -q src",
    "clean": "python -c \"import pathlib, shutil; [shutil.rmtree(p, ignore_errors=True) for p in pathlib.Path('src').rglob('__pycache__')]\""
  }
}
